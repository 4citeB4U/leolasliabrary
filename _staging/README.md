# Staging notice

This directory contains an early handoff artifact only. The live candidate is the repository root. It is not linked from the active application routes and should be removed in a later cleanup commit.
