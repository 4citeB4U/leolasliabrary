# Legacy route policy

The active Leola experience is now the repository root (`index.html`), `reader.html`, `training.html`, and `game-house.html`.

Legacy library and game HTML entry points redirect into those active routes so visitors cannot encounter the retired avatar or stale game screens.

Historical source remains recoverable through Git history. Canonical book files remain unchanged.
